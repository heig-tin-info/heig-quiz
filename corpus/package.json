{
  "name": "@quiz/corpus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Question corpus transcribed from the C / C++ course material, validated with the real question-type schemas before import.",
  "scripts": {
    "validate": "tsx tools/validate.ts",
    "import": "tsx tools/import-http.ts"
  },
  "dependencies": {
    "@quiz/contracts": "workspace:*",
    "@quiz/core": "workspace:*",
    "@quiz/domain": "workspace:*",
    "@quiz/qt-code": "workspace:*",
    "@quiz/registry": "workspace:*",
    "yaml": "^2.9.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "tsx": "^4.23.0",
    "typescript": "^5.9.3"
  }
}
