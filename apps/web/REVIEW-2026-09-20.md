# apps/web review — 2026-09-20

Read-only review of `~/quiz/apps/web` (React 19 / Vite / TanStack Query), against
`CLAUDE.md`, `.claude/skills/quiz-ui/SKILL.md`, `apps/web/DESIGN.md`, `docs/PLAN-MVP.md` §6,
`docs/spec/08-experience-deux-niveaux.md`, `docs/spec/03-exigences-non-fonctionnelles.md`,
and `mockups/*.html`.

Method: static greps + a throwaway i18n parity script; then playwright/chromium against
`VITE_MOCK=1 vite --port 5199` — **50 scenes × 3 viewports (1440×900 light, 1440×900 dark,
390×844) = 150 runs**, each collecting console/page errors, failed requests, horizontal
overflow, headings, accent-button counts and a full axe-core pass with `color-contrast`
enabled; plus targeted keyboard, focus-trap and layout probes. Nothing in `~/quiz` was
modified; port 3000 and 5173 were not used.

## Findings (ranked)

| id | sev | route | what | evidence | fix |
|---|---|---|---|---|---|
| W1 | a11y (serious) | all (30 of 50 screens, both themes) | `text-fg-faint` is used for real caption text, not just icons, and fails 4.5:1 everywhere | axe `color-contrast`, 112 page-instances: 3.14 (`#8f8a80` on `canvas`), 3.43 (on `surface`), 3.19/3.43 dark. Hits sidebar section labels, course codes, "Choose one answer.", "14 minutes left", "Explanation", live-grid type labels, and the player's `<h1>`. DESIGN.md holds `fg-faint` to 3:1 as "captions, disabled, icons at rest" — captions are text | darken `fg-faint` to ≥4.5:1 (~`#767167` light, ~`#8a857c` dark), or move caption text to `fg-muted` and reserve `fg-faint` for icons |
| W2 | a11y (critical) | `/questions/:id`, `/evaluations/:id` | `aria-controls` points at ids that do not exist — nothing renders `role="tabpanel"` or `question-panel-*` / `eval-step-panel-*` | axe `aria-valid-attr-value`, 27 instances; `ui.tsx:1596-1597` emits the ids, `QuestionEditor.tsx:307` and `EvaluationConfig.tsx:252` pass `idPrefix`, no consumer renders the panel | give the panel wrapper `id={`${idPrefix}-panel-${value}`} role="tabpanel" aria-labelledby={…-tab-…}`, or drop `idPrefix` |
| W3 | a11y (moderate) | `/pools/:id`, `/evaluations/:id/live`, `/evaluations/:id/grading` | a page-level query failure or not-found renders **no `<h1>` at all** — the page is one bare alert | `PoolView.tsx:233`, `LiveDashboard.tsx:199`, `GradingPanel.tsx:319` return `<QueryError>` before `PageHeader`. Verified: `?fail=1` and `?empty=1` pages have `document.querySelectorAll("h1").length === 0` | render `PageHeader` above the error branch, or give `QueryError` an `<h1>` when it is the page |
| W4 | a11y (moderate) | `/take/:id` (closed) | `ClosedScreen` has no heading of any level — "Time is up" is body text | `?scene=closed`: headings = `[]`; `ClosedScreen.tsx:69` passes `title` to a non-heading | render the copy title as `<h1>` |
| W5 | a11y / design | `/take/:id` (running) | the player's only `<h1>` is "Question 1 of 4" — 13 px, uppercase, `fg-faint` (3.14:1), and it changes on every navigation; the evaluation title in the top bar is not a heading | `PlayerShell.tsx`; h1 text confirmed at 1440 and 390 | make the evaluation title the `<h1>`, demote the counter to `<p>` |
| W6 | a11y / design | `/evaluations/:id/live` | the `<h1>` contains the state `Badge` **and** a per-second `Countdown`: the document heading is "Quiz 3 — pointeurs et tableaux**running11:58**" and mutates every second | `LiveHeader.tsx:64-71` puts both inside `PageHeader title` | keep only the title in `title`; move badge + countdown to `description` or the actions row |
| W7 | i18n (N-I18N-01) | `/settings` | `AvatarEditor.tsx` ships seven hard-coded English strings — a French user sees English | `:93 title="Profile picture"`, `:122` "Choose an image", `:123` "JPEG, PNG or WebP — you will crop it next", `:169` "Drag to reposition, scroll or use the slider to zoom.", `:183 aria-label="Zoom"`, `:202`/`:207` `apiErrorMessage(…, "Could not upload/remove the picture. Try again.")` | route all seven through `t()` with `en`+`fr` entries |
| W8 | i18n | global | hard-coded English accessible names | `Breadcrumb.tsx:6 aria-label="Breadcrumb"`, `notify.tsx:162 aria-label="Dismiss notification"` | `t()` |
| W9 | i18n (latent) | shared primitive | `QueryError`'s default `fallback ?? "The server did not answer."` is an English literal in `ui.tsx:1334`; its own comment says "teacher surfaces keep the English default" | all 21 call sites currently pass a `t()` fallback, so it is dead today — a landmine, not a live bug | make `fallback` required, or default it to `t("error.server")` |
| W10 | a11y (serious) | `/evaluations/:id/grading`, `/evaluations/:id/results?tab=questions`, `/attempts/:id/feedback` | horizontally-scrolling `<pre>` code blocks have no keyboard access at 390 px — a keyboard-only student cannot read past the fold | axe `scrollable-region-focusable` at 390: grading (2 nodes), results-questions (1), feedback (2); e.g. `<pre class="mt-1.5 overflow-x-auto font-mono…">` with no focusable child | `tabIndex={0}` + `role="region"` + an accessible name on the scroll container |
| W11 | a11y (minor) | `/classrooms/:id`, `/pools/:id`, `/admin`, `/questions/:id` | empty `<th>` cells | axe `empty-table-header`: `EvaluationList.tsx:210 <th className={T.th} w-10 />` and `QuestionTable.tsx:75 <th className={cx(T.th,"w-8")}>` (checkbox column) have no name at all; `<th aria-label="Actions">` is not reliably exposed as header text | put a `<span className="sr-only">` inside the `th` instead of `aria-label` |
| W12 | a11y (moderate) | `/questions/:id`, `/evaluations/:id/grading` | two unlabeled `complementary` landmarks on the same page | axe `landmark-unique`; `Shell.tsx:283` sidebar + `QuestionEditor.tsx:355` / `GradingPanel.tsx:460` / `InspectPanel.tsx:84` | `aria-label` on each `<aside>` |
| W13 | a11y (moderate) | `/questions/:id` (all four types), `/evaluations/:id/grading` | `<h3>` with no intervening `<h2>` | axe `heading-order`: "Choices", "Accepted answers", "Blanks", "Question" | use `SectionHeading` (h2) or demote to a non-heading label |
| W14 | design (spec gap) | `/questions/:id` | spec §8.5 lists `Ctrl+Enter` = "Essayer la question"; it is **not implemented** | `QuestionEditor.tsx:174-193` handles only `Ctrl+S`, `Ctrl+Shift+P`, `Ctrl+Shift+M`; the in-app hint `editor.shortcuts` omits it; verified in browser (tab stays on Edit) | add a `Ctrl+Enter` branch that switches to the Try tab, or strike it from the spec |
| W15 | design (spec gap) | `/take/:id` | `Ctrl+K` does nothing during an attempt: `App.tsx` renders `AttemptPage` outside `Shell`, and `CommandPalette` lives in `Shell` | verified: 0 dialogs after `Control+k` on the player, works everywhere else | probably deliberate (zen/exam) — say so in DESIGN.md, which currently promises "from anywhere" |
| W16 | design | `/evaluations/:id/live`, `/take/:id` | the countdown keeps ticking while an evaluation is **paused** — `Countdown` (`ui.tsx:2140`) takes only `deadlineAt`/`now` and has no paused state | verified on a real id: badge flips to `paused`, digits keep falling (11:53 → 11:50) | give `Countdown` a `frozen`/`pausedAt` prop; confirm with the API whether `closesAt` shifts on resume |
| W17 | bug (mock only) | `/evaluations/running/live` | pause/resume cannot be reviewed on the state-alias URL: the mock resolves by state, so `POST …/running/pause` → 200, then `GET …/running/dashboard` → **404**, and the dashboard silently keeps rendering the stale, still-ticking view | traced by wrapping the mock's `fetch`; works correctly on the real id `00000000-0000-4000-8000-0000000000e3` | freeze the alias→id mapping on first resolution in `mock/index.ts:2114` |
| W18 | a11y (minor) | `/questions/:id` (code) | Monaco's dimmed line numbers are `#a7c9d3` on white = **1.75:1** in the light theme | axe sample: `<div class="line-numbers lh-even dimmed-line-number">18</div>` | set the Monaco theme's line-number colour from `--fg-faint` / `--fg-muted` |
| W19 | design (minor) | `/?empty=1`, `/questions/:id?tab=try` | two accent-filled buttons on one screen | teacher home empty state: "New course" in `PageHeader` **and** in `EmptyState`; Try tab: "Publish" + "Grade my answer" | drop the header button while the list is empty (DESIGN.md allows two, but a duplicate of the same action is noise) |
| W20 | design (minor) | any teacher route, student persona | a student opening `/pools/p1` gets `StudentHome` rendered **under the teacher URL**; a reload or Back lands there again | `App.tsx` falls through to `StudentHome` for every non-student route; verified `h1 = "Hello, Léa"` at `/pools/p1` | `replaceState` to `/` when the route is not available to the role |
| W21 | design (minor) | `/evaluations/:id/live` | the dashboard has no `primary` button; its single saturated button is the destructive **Close** (`variant="danger"`), so the squint test points at the action that ends the quiz | `LiveHeader.tsx:116` | consider a quieter Close until the countdown expires |

## Verified OK

- **i18n dictionaries**: 1185 keys in `en`, 1185 in `fr`, no missing, no extra, no empty value,
  no `{var}` mismatch. (`qt.cloze.e.textHint`'s `{{answer}}`/`{{réponse}}` is cloze syntax, not
  interpolation.) No French prose in `en`, no English prose in `fr` — the heuristic hits were all
  cognates ("Question", "Points", "Code", "Administration").
- **No raw colour anywhere**: zero `dark:` variants, zero hex, zero Tailwind palette scales in
  `apps/web/src/**` and `packages/qt-*/src/**`. The one `#000` (`ui.tsx:1559`) is a gradient mask,
  which DESIGN.md sanctions.
- **No `shadow-*` in the page flow**; no `window.confirm` / `alert` / `prompt`.
- **No `console.error`, no `TODO`/`FIXME`/`HACK`** in shipped code. Two `console.warn`: one
  `import.meta.env.DEV`-gated (`useEventStream.ts:93`), one mock-only.
- **Zero console errors, zero uncaught page errors, zero failed requests** across all 150 runs.
- **Zero horizontal page overflow** at 1440×900 and 390×844, light and dark, on all 50 scenes.
- **Routes**: every `Route` member in `router.ts` has a screen in `App.tsx` and vice versa;
  `devUi` is correctly `import.meta.env.DEV`-gated.
- **`<html lang>` follows the locale** (`i18n.tsx:2613`).
- **`text-on-fill` works**: Publish in dark mode is `#131211` on `#e85f64`; axe found no
  ink-on-fill failure anywhere, in either theme.
- **Layer contract holds**: focus moves into the panel on open, stays trapped through 25 Tabs,
  returns to the trigger on Escape; a backdrop click does **not** close a form layer;
  `aria-modal="true"` and `aria-labelledby` resolve to a real title.
- **`Ctrl+K`** opens the palette from anywhere inside the Shell, including from inside a text
  field, and the contextual live commands (Pause, +5 minutes, Close) register on the dashboard.
- **Player keys**: `Alt+→` / `Alt+←` navigate between questions; `Ctrl+Enter` marks done.
- **Dashboard keys**: `n` / `r` / `s` toggle names / answers / results; `f` toggles full screen;
  `Space` pauses and resumes (on a real id — see W17); `Escape` closes the inspect panel.
- **Grading keys**: `v` validates and moves on (queue 5 → 4, score 14/20 → 15/20); `→` / `←`
  move; `o` opens the override sheet; Escape closes it. All guarded against firing inside a
  field or behind an open dialog.
- **Five async states** render on pool, live, grading, results and student home
  (`?slow=1` skeletons, `?empty=1`, `?fail=1`, `—` for partial rows, success).
- Every one of the 21 `QueryError` call sites passes a translated `fallback`.

## Design gaps vs mockups

Only material differences; the screens are otherwise faithful.

1. **Grading panel matches `04-correction.html`** (question aside + inline-expanding entry list).
   It is `docs/PLAN-MVP.md` §6.2's wording — "`EntryList` (left), `EntryDetail` (right)" — that is
   stale, not the screen. Worth correcting the plan so the next reader does not "fix" the layout.
2. **Pool (`08-pool.html`)**: the type badges wrap to two lines at 1440 ("Multiple choice",
   "Fill in the blanks", "Short answer") where the mockup keeps them on one. Widen the column or
   shorten the labels.
3. **Projection view (`10-sondage-projection.html`)** has no route of its own; the dashboard's
   `f` / full-screen toggle is what covers it. Fine, but the mockup's index implies a screen.
4. Live dashboard, zen player, lobby, student home, editors and the command palette match
   `03`, `07`, `06`, `05`, `01`/`02` and `09` closely — nothing material to report.

## Notes for the next reviewer

- `apps/web/scripts/screenshots.mjs` already deep-links every scene; its `TAKE`, `ATTEMPT_OPEN`,
  `ATTEMPT_PAST` constants and the `/evaluations/<state>/…` aliases are the fastest way in.
- Pause/resume must be exercised on a real evaluation id, not on the `running` alias (W17).
- Screenshots from this pass: `<scratchpad>/shots/` (~120 PNGs); raw data `<scratchpad>/audit.json`.
