# heig-codespace: closed network, no resolver.
# Without these options the libc falls back to 127.0.0.1 and waits 5 s per
# attempt, which makes curl, git and clangd hang on every failed resolution.
options timeout:1 attempts:1
